apiVersion: 1

providers:
  - name: 'OpenRE Dashboards'
    orgId: 1
    folder: 'OpenRE'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

[package]
name = "openre-ai"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
description = "AI service (providers, prompts, tools) for open-re"
categories = { workspace = true }
keywords = { workspace = true }

[dependencies]
openre-core = { path = "../openre-core" }
openre-config = { path = "../openre-config" }
reqwest = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
dashmap = "5.5"
regex = "1.10"
futures = { workspace = true }
tokio-stream = { workspace = true }
async-trait = { workspace = true }
sha2 = { workspace = true }
tracing = { workspace = true }
base64 = { workspace = true }
hex = { workspace = true }
bytes = { workspace = true }

# Local AI (optional - disabled for now due to dependency issues)
# ort = { version = "1.16", features = ["load-model", "dynamic"], optional = true }
# llama-cpp-2 = { version = "0.1", features = ["metal", "cuda", "vulkan", "blas"], optional = true }
# tokenizers = { version = "0.15", optional = true }
# candle-core = { version = "0.5", optional = true }
# candle-nn = { version = "0.5", optional = true }
# candle-transformers = { version = "0.5", optional = true }

[features]
default = ["cloud-ai"]
# local-ai = ["dep:ort", "dep:llama-cpp-2", "dep:tokenizers", "dep:candle-core", "dep:candle-nn", "dep:candle-transformers"]
cloud-ai = []
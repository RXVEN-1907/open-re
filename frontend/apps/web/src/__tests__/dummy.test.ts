// Dummy test to prevent vitest from failing when no test files exist
import { describe, it, expect } from 'vitest';

describe('Dummy', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
